# Operators in Python

## What is an Operator?

An **operator** is a symbol that performs an operation on one or more values.

Example:

```python
3 + 4
```

Here,

- `3` and `4` → **Operands**
- `+` → **Operator**
- `3 + 4` → **Operation (Expression)**

---

# Types of Operators in Python

Python provides the following types of operators:

### 1. Arithmetic Operators

Used to perform mathematical calculations.

Examples:

```python
+   -   *   /   //   %   **
```

---

### 2. Comparison Operators

Used to compare two values.

Examples:

```python
==   !=   >   <   >=   <=
```

Returns either **True** or **False**.

---

### 3. Logical Operators

Used to combine multiple conditions.

Examples:

```python
and
or
not
```

---

### 4. Assignment Operators

Used to assign values to variables.

Examples:

```python
=
+=
-=
*=
/=
```

---

### 5. Bitwise Operators

Perform operations on the binary representation of numbers.

Examples:

```python
&
|
^
~
<<
>>
```

---

### 6. Membership Operators

Check whether a value exists in a sequence.

Examples:

```python
in
not in
```

---

### 7. Identity Operators

Check whether two variables refer to the same object in memory.

Examples:

```python
is
is not
```

---

# 30-Second Revision

- **Operand** → Value on which an operation is performed.
- **Operator** → Symbol that performs the operation.
- **Operation (Expression)** → Combination of operands and an operator.

### Types of Operators

- Arithmetic
- Comparison
- Logical
- Assignment
- Bitwise
- Membership
- Identity